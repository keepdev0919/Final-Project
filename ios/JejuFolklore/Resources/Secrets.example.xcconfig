// 이 파일을 복사해서 같은 폴더에 `Secrets.xcconfig` 로 저장한 뒤
// 각자 발급받은 키로 값을 채운다. Secrets.xcconfig 는 .gitignore 되어 있다.
//
// 새 컴퓨터 셋업:
//   cp Secrets.example.xcconfig Secrets.xcconfig
//   # 그 다음 Secrets.xcconfig 를 열어 키 값을 직접 채운다

GOOGLE_MAPS_API_KEY = YOUR_KEY_HERE
