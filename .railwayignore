# Railway 업로드에서 뺀다 — 서버 이미지에 들어가지 않는 것 (.dockerignore 와 같은 기준).
# 업로드가 가벼워져 배포가 빨라진다.
ios/
docs/
submissions/
scripts/
tests/
backend/tests/
backend/venv/
storage/vector_db/
storage/tts_cache/
data/exports/
data/processed/
data/raw/

# 앱인토스 미니앱(웹) — 서버 이미지와 무관하고 node_modules 가 커서 업로드에서 뺀다.
miniapp/

# 운영 서버에서 꺼낸 기록 백업 — 서버에 다시 올리지 않는다.
storage/backups/
