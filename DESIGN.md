# 놀멍봅서 디자인 시스템

**서비스명:** **놀멍봅서** — 제주어 "천천히 보세요". 우리 제품이 하는 일(무엇을 보라고 짚어주기 + 내 속도로 걷기)이 이름에 그대로 들어 있다.
**작성일:** 2026-08-13 · **상태:** v1 (이름·색감 확정 2026-08-13)
**연관:** [`docs/기획/설계_현장경험_엔진_v1.md`](docs/기획/설계_현장경험_엔진_v1.md) · 방향 SSoT [`docs/기획/방향성_재정립_2026-07-20.md`](docs/기획/방향성_재정립_2026-07-20.md)

> 이 문서가 **디자인의 단일 진실원**이다. 화면을 만들 때 색·글자·여백·테두리를 여기서 가져온다.
> 여기 없는 값을 즉석에서 만들지 않는다. 필요하면 이 문서에 먼저 추가한다.

---

## 1. 미감 — 한 문장

> **픽셀아트로 만든 게임 같은 앱 안에, 제주의 실제 풍경 사진이 들어앉는다.**

**색감 방향 = 제주 파스텔 픽셀** (2026-08-13 확정). 밝고 부드러운 제주 자연색으로, 실사 사진과 채도가 비슷해 서로 튀지 않는다. 검토했다가 접은 두 방향:
- **게임보이 레트로**(5색 저채도) — 제주어의 옛 결과는 맞지만, 실사 사진이 들어가면 사진만 도드라진다
- **픽셀 + 손그림 혼합** — 정겨움은 가장 살지만 톤 관리가 어렵고 그림 파일이 더 필요하다

### 로고

**"놀멍봅서" 4글자를 2줄로 쌓는다.** 한 줄로 늘어놓으면 정사각 아이콘 안에서 가늘어진다.

```
┌─────────┐
│ 놀   멍 │   2줄 = 정사각형을 꽉 채움
│ 봅   서 │   픽셀 폰트는 글자 폭이 일정해 줄이 딱 맞는다
└─────────┘
```

레퍼런스는 둘이다. **싸다보카** — 픽셀 도트 세계관과 캐릭터. **투어라이브** — 오디오 가이드의 화면 구조와 정보 배치. 앞의 것에서 보이는 방식을, 뒤의 것에서 구조를 가져온다.

### 절대 규칙 — 픽셀과 실사의 경계

| 픽셀아트로 | 실사 사진으로 |
|---|---|
UI 셸 — 프레임·버튼·아이콘·진행바·락 배지·탭바 | 장소 사진, 카드 썸네일, 커버 |
가이드 캐릭터, 배경, 홈 세계관 | **참고사진** (시선 유도용) |
지도 마커·핀, 참고사진 위의 표시 마커 | |

**사진에 픽셀 필터를 씌우지 않는다.** 성산의 절벽과 분화구는 실제로 보여야 한다. 사진을 도트로 바꾸면 "무엇을 보라"는 안내가 무의미해진다. 사진은 원본 그대로, **픽셀 테두리 프레임 안에** 넣는다. 이 대비가 오히려 컨셉을 선명하게 만든다.

### 화면을 봐도 되는 곳 / 안 되는 곳

> **⚠️ 2026-08-20 정정.** 여기 원래 *"북극성은 폰을 적게 보기다"* 라고 적혀 있었다.
> 위치 기반 퀘스트 게임으로 피벗하면서 북극성이 **"앱이 사람을 그 자리에 서서,
> 실제로 그것을 보게 만든다"** 로 바뀌었다 → `docs/기획/설계_위치기반_퀘스트게임_v2.md` §1.
> 게임은 화면을 본다. 다만 **걸으면서 보게 만들지 않는다** (안전 문제이기도 하다).

화면을 보는 순간을 **도착·수집·선택**에 모은다. 걷는 동안은 소리만 나온다.

| 마음껏 꾸미는 곳 | 최소한으로 두는 곳 |
|---|---|
홈·탐험(루트 선택)·지도 | **해설 재생 중 — 걷는 구간** |
수집 순간·배지·완료 기록 | 시선 유도 순간 (참고사진) |

---

## 2. 색

제주에서 실제로 보이는 것에서 가져왔다. 픽셀아트는 색 수가 적을 때 좋아 보이므로 **아래 팔레트 밖의 색을 쓰지 않는다.**

### 라이트

| 역할 | 값 | 어디에 |
|---|---|---|
배경 | `#F4EFE4` | 화면 바탕 (모래빛) |
표면 | `#FFFFFF` | 카드·시트 |
잉크 | `#1E1B18` | 본문·테두리 (현무암) |
잉크 약함 | `#6B635A` | 보조 설명·비활성 |
**주색** | `#1E6F6B` | 주요 버튼·선택 상태 (성산 앞바다) |
**강조** | `#F2B233` | CTA·무료 배지·진행 완료 (유채) |
잠김 | `#C9503C` | 락 배지·경고 (화산 적갈) |
완료 | `#4E8C3F` | 들은 이야기 표시 (억새) |

### 다크

| 역할 | 값 |
|---|---|
배경 | `#14181A` |
표면 | `#1E2427` |
잉크 | `#EDE7DC` |
잉크 약함 | `#9A9187` |
주색 | `#4FB3AD` |
강조 | `#FFC759` |
잠김 | `#E0705A` |
완료 | `#6FAE5E` |

**두 모드를 모두 지원한다.** 현장에서 새벽 일출이나 저녁에 쓰는 앱이라 어두운 환경 사용이 실제로 잦다.

### 명도 대비 실측 (2026-08-14)

측정했다. `tests/test_design_tokens.py`가 `PixelColor.swift`를 읽어 매번 검증하므로, 색을 바꾸면 이 테스트가 먼저 잡는다.

| 조합 | 라이트 | 다크 | 기준 |
|---|---|---|---|
| 잉크 / 배경 | 14.95 | 14.52 | 4.5 |
| 잉크 / 표면 | 17.14 | 12.76 | 4.5 |
| 잉크 약함 / 배경 | 5.15 | 5.76 | 4.5 |
| 잉크 약함 / 표면 | 5.90 | 5.07 | 4.5 |
| 배지 글자 / 강조 | 9.13 | 11.07 | 3.0 |
| 배지 글자 / 잠김 | 3.83 | 5.42 | 3.0 |
| 배지 글자 / 완료 | 4.20 | 6.44 | 3.0 |
| 표면 / 주색 | 5.93 | 6.27 | 3.0 |

배지는 Galmuri9를 18pt로 쓰는 굵은 픽셀 글자라 large text 기준 3:1을 적용한다.

⚠️ **측정에서 결함을 하나 찾았다.** 다크 모드에서 강조색 `#FFC759` 위에 잉크 `#EDE7DC`를 올리면 **1.26:1**로 글자가 사실상 사라진다. 밝은 배지 배경 위 글자는 모드와 무관하게 어두운색으로 고정해 해결했다 — §6 배지 참조.

### 색 사용 규칙

- 그라디언트를 쓰지 않는다. 픽셀아트는 단색 면으로 구성한다.
- 반투명을 최소화한다. 겹칠 때는 단색으로 처리한다.
- 색만으로 상태를 구분하지 않는다 — 락은 색 + 자물쇠 모양, 완료는 색 + 체크 모양을 함께 쓴다.

---

## 3. 글자

### 폰트

**갈무리(Galmuri)** 를 쓴다. 닌텐도 DS 폰트에서 영감받은 한글 픽셀 폰트이고 **SIL 오픈 폰트 라이선스 1.1**이라 상업적 이용이 가능하다.
대안은 **물마루(Mulmaru)** — 게임용 한글 픽셀 폰트이며 상업적 이용 가능(폰트 파일 단독 판매만 금지).

⚠️ 적용 전에 라이선스 원문과 앱 임베딩 조건을 한 번 더 확인한다.

### 비트맵 폰트의 크기 제약 ★중요

갈무리는 `Galmuri7 / 9 / 11 / 14`처럼 **설계 픽셀 높이별로 나뉜다.** 비트맵 폰트는 설계 크기의 정수배로 렌더링할 때만 선명하고, 어중간한 크기에서는 흐려진다.

**따라서 글자 크기를 임의로 정하지 않는다.** 아래 단계만 쓴다.

| 용도 | 폰트 | 크기 |
|---|---|---|
화면 제목 | Galmuri11 | 22 (11×2) |
카드 제목 | Galmuri11 | 22 |
본문·라벨 | Galmuri11 | 11 또는 22 |
버튼 | Galmuri11 | 22 |
작은 설명·배지 | Galmuri9 | 18 (9×2) |
숫자 강조 (시간·가격) | Galmuri14 | 28 (14×2) |

### 긴 글은 픽셀 폰트를 쓰지 않는다

픽셀 폰트로 긴 문단을 읽으면 눈이 빨리 피로해진다. 아래는 **시스템 폰트(Apple SD Gothic Neo)** 를 쓴다.

- 이야기 스크립트 전문 보기
- 이용약관·개인정보 처리방침
- 오류 상세 설명

컨셉을 깨는 게 아니다. 프레임과 버튼은 여전히 픽셀이고 안쪽 글만 읽기 편한 폰트다. 접근성에도 이게 맞다.

⚠️ **동적 글자 크기(Dynamic Type)** 를 지원한다. 다만 픽셀 폰트는 정수배로만 커져야 하므로, 단계를 뛰어 확대한다(22 → 33 → 44).

---

## 4. 여백과 격자

**모든 간격은 4의 배수다.** 픽셀 격자에서 벗어나면 선이 흐려진다.

```
4   8   12   16   24   32   48   64
```

| 용도 | 값 |
|---|---|
아이콘과 라벨 사이 | 8 |
카드 안쪽 여백 | 16 |
카드 사이 | 12 |
섹션 사이 | 32 |
화면 좌우 여백 | 16 |
버튼 높이 | 48 |
탭바 높이 | 56 |

---

## 5. 모양

### 모서리와 테두리

- **모서리 반경은 0이다.** 예외 없다. 둥근 모서리는 픽셀아트를 즉시 깨뜨린다.
- 테두리는 잉크색 실선, **전부 4px**.

> **2026-08-20 변경.** 원래 버튼 2px / 카드 3px이었다. **4px로 통일했다.**
> ① 2·3px은 "모든 간격은 4의 배수"(§4)를 벗어난 유일한 값이었다.
> ② 3px 테두리는 웹 카드처럼 보였다. 4px이 되면 게임 상자로 읽힌다.
> 시뮬레이터에서 나란히 놓고 확인했다.

### 코너 악센트 ★ 2026-08-20 추가

카드·시트의 **네 귀퉁이에 8×8 잉크 사각형**을 박는다. 테두리 두께의 2배이고,
**테두리 밖으로 절반(4px) 빼서** 놓는다.

```
■────────────────■     ← 귀퉁이 점이 테두리 밖으로 튀어나온다
│                │
│   성산일출봉      │
│                │
■────────────────■
```

**이게 RPG 상자 인상을 만드는 장치다.** 테두리만 있으면 아무리 두꺼워도 웹 카드로
보인다. 귀퉁이에 점을 찍으면 게임 UI 프레임이 된다.

⚠️ **점을 테두리 안쪽에 두면 잉크 위 잉크가 되어 아예 안 보인다** (실제로 겪음).
반드시 밖으로 빼야 한다.

배지나 작은 버튼에는 붙이지 않는다 — 지저분해진다. 카드·시트처럼 큰 박스만.

### 그림자

번지는 그림자를 쓰지 않는다. **어긋난 단색 그림자**를 쓴다.

```
x: +4, y: +4, blur: 0, 색: 잉크
```

버튼을 누르면 그림자가 사라지고 버튼이 4px 내려간다. 눌린 느낌이 이것만으로 난다.

### 아이콘

⚠️ **SF Symbols를 쓰지 않는다.** 애플 기본 아이콘은 픽셀아트가 아니라서 섞이면 어색하다. 필요한 아이콘을 픽셀로 직접 만든다.

필요한 아이콘 목록: 재생 · 멈춤 · 다음 · 사진 · 자물쇠 · 체크 · 위치핀 · 다운로드 · 홈 · 지도 · 사람 · 뒤로.
크기는 16 또는 24(둘 다 4의 배수).

---

## 6. 컴포넌트 규칙

### 장소 카드 (홈 최상단)

```
┌───────────────────────────┐  ← 테두리 4, 반경 0, 그림자 +4/+4, 코너 악센트
│ [실사 사진]                │
│                           │
├───────────────────────────┤
│ 성산일출봉                  │  ← Galmuri11 / 22
│ 아름다움 뒤의 이야기         │  ← Galmuri9 / 18, 잉크 약함
│ 이야기 5개 · 50분           │
│ ┌────────┐  ┌──────────┐  │
│ │앞부분 무료│  │지금 여기예요│  │  ← 배지: 강조 / 주색
│ └────────┘  └──────────┘  │
└───────────────────────────┘
```

### 배지

| 종류 | 배경 | 글자 | 아이콘 |
|---|---|---|---|
앞부분 무료 | 강조 | **고정 어두운색 `#1E1B18`** | 없음 |
지금 여기예요 | 주색 | 표면 | 위치핀 |
**해설 3분 7초** | 표면 | 잉크 | **재생 ▶** |
들었어요 | 완료 | **고정 어두운색 `#1E1B18`** | 체크 ✔ |
잠김 | 잠김 | **고정 어두운색 `#1E1B18`** | 자물쇠 |

⚠️ **"들을 게 있다"에 체크(✔)를 쓰지 않는다.** 체크는 "다 들었다"는 뜻이다.
아이콘이 상태를 거짓으로 말하면 색보다 더 빨리 오해를 만든다 (2026-08-20 수정).

⚠️ **밝은 배지 배경(강조·잠김·완료) 위 글자는 다크 모드에서도 어두운색으로 고정한다.**
다크 모드 잉크(`#EDE7DC`)를 강조색 위에 올리면 1.26:1이 되어 글자가 사라진다(§2 실측).
코드에서는 `PixelColor.inkFixedDark`를 쓴다. 주색만 어두운 계열이라 표면색 글자를 쓴다.

### 막대는 두 종류다 ★ 2026-08-20 정리

**셀 수 있는 것과 정도는 다르게 그린다.** 섞으면 둘 다 거짓말이 된다.

**① 칸으로 나뉜 막대 — 셀 수 있는 것** (`PixelProgressBar`)

```
■ ■ ■ □ □      ← 이야기 5개 중 3개 들음
```

들은 이야기가 늘면 칸이 하나씩 채워진다. **몇 개 남았는지가 바로 보인다.**
칸 높이는 24px — 테두리가 4px이라 그보다 낮으면 안이 안 보인다.

**② 채워지는 막대 — 정도** (`PixelMeter`)

```
┃████████▌            ┃   ← 난이도, 하루 이동거리
```

채운 끝을 잉크 4px로 끊어 준다 (게임 체력바의 그 인상).

**⚠️ 셀 수 있는 걸 ②로 그리면** 몇 개 남았는지 안 보인다.
**정도를 ①로 그리면** 없는 눈금을 만들어낸다. 예: 난이도를 5칸으로 그리면
사용자가 "5단계 중 3단계"로 읽는데, 실제로는 연속값(하루 이동거리 km)이다.

### 참고사진 마커

실사 사진 위에 픽셀 마커를 올린다. 투어라이브가 빨간 원으로 대상을 표시하는 방식을, 픽셀로 한다.

- 원: **2px** 테두리 원, 채우기 없음, 잠김색
- 화살표: 8×8 픽셀 삼각형
- 라벨: 표면 배경 + **2px** 테두리 박스, Galmuri9 / 18

> **여기만 4px 규칙의 예외다.** 마커는 실사 사진 **위에** 올라가 대상을 가리키는
> 것이라, 4px이면 정작 봐야 할 것을 덮는다. UI 셸(카드·버튼)과 목적이 다르다.

원 하나에 라벨 하나를 붙인다. 한 사진에 마커는 **3개까지**만 — 그 이상이면 무엇을 봐야 할지 흐려진다.

### 재생 화면

여기만 규칙이 다르다. **장식을 넣지 않는다.**

```
        [이야기 3] 등경돌
     ■ ■ ■ □ □

     ┌─────────────┐
     │    ▶ 재생    │      ← 버튼 하나만 크게
     └─────────────┘

   [ 참고사진 ]  [ 다음 지점 ]
```

배경은 단색. 캐릭터를 띄우지 않는다. 사용자는 풍경을 봐야 한다.

---

## 7. 움직임

픽셀아트는 부드럽게 움직이면 어색하다. **단계적으로** 움직인다.

- 전환 시간 100~150ms, 이징은 단계형(step) 또는 선형
- 흐림·번짐 효과를 쓰지 않는다
- 캐릭터는 2~3프레임 반복 애니메이션 (숨 쉬는 정도)
- 화면 전환은 슬라이드보다 **즉시 교체**가 컨셉에 맞다

⚠️ **"동작 줄이기(Reduce Motion)"** 설정을 켠 사용자에게는 애니메이션을 끈다.

---

## 8. 필요한 그림 파일

코드로 만들 수 없어 누군가 그려야 하는 것. **아래 3개가 전부다.**

| 무엇 | 사양 | 없으면 |
|---|---|---|
**가이드 캐릭터 (곱닥이)** | 정면 기본 + 말하는 중 + 기뻐하는 중, 3종. 투명 배경 PNG, 64×64 기준 (2배·3배 함께) | 홈이 비어 보임. 대체 불가 |
**앱 아이콘** | 1024×1024 (=128×8). 아래 규칙 참조 | **앱스토어 제출 불가** |
홈 배경 | 성산 실루엣 정도의 단순한 픽셀 배경 | 단색으로도 성립. 후순위 |

나머지 — 테두리·버튼·진행바·배지·마커·아이콘 — 는 **코드로 만든다.** 그림 파일이 필요 없다.

### 이미지 생성 모델을 쓸 때

캐릭터와 배경은 생성 모델로 만들어도 된다. 단 역할을 나눈다.

| 대상 | 방법 |
|---|---|
| 캐릭터·배경 | 생성 모델 ⭕ |
| **"놀멍봅서" 글자** | 생성 모델 ❌ → **갈무리 폰트로 직접 조판.** 이미지 모델은 한글을 제대로 쓰지 못한다 |

⚠️ **생성 모델은 "픽셀아트처럼 보이는 고해상도 그림"을 만든다.** 도트 격자가 정수로 맞지 않고, 색이 수백 개 섞이며, 경계가 흐리다. 작게 보면 괜찮지만 아이콘은 1024로 크게 쓰이므로 드러난다. **후처리가 필수다:**

1. 생성물을 작게 줄인다 (128×128)
2. 색을 §2 팔레트로 축소·고정한다
3. **정수배로** 다시 확대한다 (128 × 8 = 1024, 부드럽게 늘리지 말고 계단식으로)

### 앱 아이콘 규칙 (놓치면 앱스토어 거절)

- **투명 배경(알파 채널) 금지** — 배경을 반드시 색으로 채운다
- **모서리를 둥글게 만들지 않는다** — 애플이 자동으로 깎는다. 미리 깎으면 이중으로 깎여 이상해진다. **정사각으로 제출**
- 픽셀아트는 모서리가 깎이면 도트가 어긋나 보이므로 **글자를 중앙에 두고 가장자리 여백을 넉넉히** 둔다

캐릭터는 직접 그리기 / 외주 / 생성 도구 중 어느 쪽이든 된다. 다만 **팔레트(§2)와 크기 격자(64의 배수)를 지켜야** 앱 안에서 겉돌지 않는다.

---

## 9. 확인이 필요한 것

| 항목 | 왜 |
|---|---|
픽셀 폰트 실제 렌더링 확인 | iOS에서 정수배 크기가 선명한지 |
캐릭터 준비 방법 | 임계경로 여부 판단 |

## 이력

- **2026-08-13:** 최초 작성. 픽셀아트 셸 + 실사 사진 방침, 제주 팔레트, 갈무리 폰트, 4px 격자, 반경 0 확정.
- **2026-08-20:** 테두리·그림자를 **4px로 통일**(§5) — 2·3px은 4배수 격자를 벗어난 유일한 값이었고, 3px은 웹 카드처럼 보였다. **코너 악센트 추가**(§5) — 카드 귀퉁이 8×8 잉크 사각형을 테두리 밖으로 반쯤 빼서 놓는다. 이게 RPG 상자 인상을 만든다. **막대를 두 종류로 정리**(§6) — 셀 수 있는 것(칸)과 정도(채워지는 막대)를 구분. 배지에 "해설 N분"(재생 아이콘) 추가. 참고: 이 촉감 체계는 Stitch가 만든 시안에서 가져와 우리 팔레트·폰트로 갈아입힌 것이다.
- **2026-08-14:** 팔레트 명도 대비 실측(§2) — 다크 모드 강조 배지가 1.26:1이던 결함을 찾아 배지 글자색을 모드 무관 고정 어두운색으로 변경(§6). 갈무리 라이선스 확인 완료(SIL OFL 1.1, 앱 임베딩 가능 · https://github.com/quiple/galmuri). 팔레트·간격을 `Views/DesignSystem/`에 코드로 옮김.
