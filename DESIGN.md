# 놀멍봅서 디자인 시스템

**서비스명:** **놀멍봅서** — 제주어 "천천히 보세요".
**작성일:** 2026-08-13 · **개정:** 2026-08-20 (v2 — 시안 기준으로 모양·글자 전면 개편)
**연관:** 방향·설계 SSoT [`docs/기획/설계_위치기반_퀘스트게임_v2.md`](docs/기획/설계_위치기반_퀘스트게임_v2.md)

> 이 문서가 **디자인의 단일 진실원**이다. 화면을 만들 때 색·글자·여백·테두리를 여기서 가져온다.
> 여기 없는 값을 즉석에서 만들지 않는다. 필요하면 이 문서에 먼저 추가한다.

> **2026-08-20 개정 근거.** 조익준님이 Stitch 시안 4개(임무 일지·인벤토리·스토리 체험·루트 선택)를
> 컨셉으로 확정했다. §3(글자)·§5(모양)·§6(컴포넌트)를 **시안 코드의 실제 CSS 값**에 맞춰 다시 썼다.
> 이전 판은 테두리 4px·픽셀 폰트였는데 시안은 **테두리 2px + 큰 그림자 + 산세리프**였다.
> 시뮬레이터에서 나란히 놓고 확인한 결과 이전 판이 뭉툭했다.

---

## 1. 미감 — 한 문장

> **RPG 퀘스트 일지처럼 생긴 화면 안에, 제주의 실제 사진이 들어앉는다.**

게임 UI의 **문서 느낌**이다 — 두꺼운 상자가 아니라, 얇은 테두리에 큰 그림자가 붙은 종이.
섹션마다 아이콘과 밑줄이 반복되고, 정보가 촘촘히 쌓인다.

레퍼런스는 둘. **Stitch 시안 4종** — 상자·섹션 헤더·칩·막대의 촉감.
**투어라이브** — 오디오 가이드의 화면 구조와 정보 배치.

### 절대 규칙 — 도트와 실사의 경계

| 도트로 | 실사 사진으로 |
|---|---|
UI 셸 — 프레임·버튼·아이콘·막대·배지·탭바 | **장소 썸네일, 카드 이미지, 커버** |
가이드 캐릭터 초상, 지도 마커·핀 | **참고사진** (시선 유도용) |
수집물 아이콘 | |

**사진에 픽셀 필터를 씌우지 않는다.** 성산의 절벽과 분화구는 실제로 보여야 한다.
도트로 바꾸면 "무엇을 보라"는 안내가 무의미해진다. 사진은 원본 그대로, **테두리 프레임 안에** 넣는다.

> **2026-08-20 결정.** 시안은 카드 이미지를 AI 픽셀 풍경화로 그렸지만 **우리는 실사로 간다.**
> 189곳에 그림을 그릴 수 없고, 실사가 "그 장소를 알아보게 하는" 기능을 한다.
> 출처는 **한국관광공사 `detailImage2`** — 장소당 4~6장을 준다(섭지코지 4장·천지연 6장 실측).
> 예외: **해설 재생 화면의 배경**만 장소별 픽셀 풍경화를 쓴다(§6).

### 화면을 봐도 되는 곳 / 안 되는 곳

북극성은 **"앱이 사람을 그 자리에 서서, 실제로 그것을 보게 만든다"** 다 (→ 설계 v2 §1).
게임이니 화면을 본다. 다만 **걸으면서 보게 만들지 않는다.**

| 마음껏 꾸미는 곳 | 최소한으로 두는 곳 |
|---|---|
홈 · 탐험(루트 선택) · 지도 | **해설 재생 중 — 걷는 구간** |
수집 순간 · 배지 · 완료 기록 | 시선 유도 순간 (참고사진) |

---

## 2. 색 ★ 2026-08-20 시안 팔레트를 그대로 가져왔다

라이트 값은 **Stitch 시안 CSS에서 복사**한 것이다. 시안이 라이트 전용이라 다크는 시안이 함께
준 `*-fixed-dim`·`inverse-*` 값을 써서 Material 관례대로 파생시킨 뒤 대비를 실측했다.

### 면 (표면 단계)

| 역할 | 라이트 | 다크 | 어디에 |
|---|---|---|---|
배경 | `#FBF8FF` | `#12141F` | 화면 바탕 |
표면 | `#FFFFFF` | `#22243A` | 카드·대화상자 |
표면 낮음 | `#F4F2FF` | `#1B1D2E` | 섹션 안쪽 |
표면 중간 | `#EDECFF` | `#282B44` | 강조 섹션 (별칭 `sunk`) |
표면 높음 | `#E6E6FD` | `#30334E` | 눌린 상태·호버 |
표면 변형 | `#E1E1F7` | `#3A3D58` | 인용 박스 |
표면 흐림 | `#D8D8EF` | `#0D0F18` | 비활성 |

### 글자 · 선

| 역할 | 라이트 | 다크 |
|---|---|---|
잉크 | `#181A2A` | `#F0EFFF` |
잉크 약함 | `#3D4A3F` | `#9EA8A0` |
선 | `#6D7B6E` | `#8A9A8B` |
선 약함 | `#BCCABC` | `#4A5A4B` |

### 색 역할 — 각각 짝이 되는 글자색을 갖는다

| 역할 | 라이트 배경 / 글자 | 다크 배경 / 글자 | 어디에 |
|---|---|---|---|
**주색 (초록)** | `#006D39` / `#FFFFFF` | `#51E088` / `#00210D` | 주요 버튼·선택 |
주색 컨테이너 | `#38CC77` / `#005129` | `#005229` / `#70FDA2` | 완료·수집 (별칭 `done`) |
**보조 (파랑)** | `#0062A2` / `#FFFFFF` | `#9DCAFF` / `#001D35` | 섹션 헤더 강조·정보 |
보조 컨테이너 | `#54ABFD` / `#003E69` | `#00497C` / `#D1E4FF` | 정보 칩 |
**삼차 (금색)** | `#735C00` / `#FFFFFF` | `#EEC215` / `#231B00` | 보상·강조 텍스트 |
삼차 컨테이너 | `#D9AF00` / `#554400` | `#574500` / `#FFE085` | 스티커 배지 (별칭 `accent`) |
삼차 고정 | `#FFE085` / `#231B00` | 같음 | 칩 (모드 무관) |
**오류** | `#BA1A1A` / `#FFFFFF` | `#FFB4AB` / `#93000A` | 잠금·경고 (별칭 `locked`) |

> **⚠️ 채움색 위 글자는 반드시 짝을 쓴다.** 고정색 하나로 두면 한쪽 모드에서 글자가 사라진다.
> 라이트 주색(`#006D39`)은 어두운 초록이라 흰 글자가 맞고, 다크 주색(`#51E088`)은 밝은 초록이라
> 어두운 글자가 맞다. 08-14에 세운 "밝은 배경 위 글자는 어두운색으로 고정"은 시안 색으로
> 갈아타면서 무효가 됐다.

### 의미 별칭

화면 코드는 "무슨 색"이 아니라 "무슨 뜻"으로 쓴다. 색을 바꿀 때 화면을 안 건드리려면 이 층이 필요하다.

| 별칭 | 가리키는 역할 |
|---|---|
`sunk` | 표면 중간 |
`accent` / `onAccent` | 삼차 컨테이너 (금색) |
`done` / `onDone` | 주색 컨테이너 (밝은 초록) |
`locked` / `onLocked` | 오류 |

**별칭이 색을 직접 들고 있으면 안 된다** — 팔레트가 두 곳에 생기고 한쪽만 바뀐다.
`tests/test_design_tokens.py`가 감시한다.

### 명도 대비 실측 (2026-08-20)

`tests/test_design_tokens.py`가 `PixelColor.swift`를 읽어 매번 검증한다.

| 기준 | 값 | 적용 대상 |
|---|---|---|
본문 | **4.5:1** | 잉크·잉크 약함 |
배지·버튼 글자 | **3:1** | 14pt 굵은 글자 (WCAG large text) |
**선·아이콘** | **3:1** | **WCAG 1.4.11 비텍스트 대비** |

라이트 `선`(`#6D7B6E`)이 표면 대비 **4.46**이다. 본문 기준으로는 미달이지만 **글자가 아니라 선**이라
3:1 기준을 적용한다. 기준을 잘못 대서 시안 색을 억지로 바꾸면 맞추려던 것에서 멀어진다.

짝 색 대비는 라이트 4.55~13.23, 다크 5.51~13.23으로 전부 3:1을 넘는다.

### 색 사용 규칙

- 그라디언트를 쓰지 않는다. 단색 면으로 구성한다.
- 반투명을 최소화한다. 겹칠 때는 단색으로.
- **색만으로 상태를 구분하지 않는다** — 잠김은 색+자물쇠, 완료는 색+체크, 재생 가능은 색+▶.

---

## 3. 글자 ★ 2026-08-20 전면 개편

### 픽셀 폰트를 UI 본문에서 뺀다

> **이전 판:** 갈무리(Galmuri) 픽셀 폰트를 화면 전체에 썼다.
> **바뀐 이유:** 시안 4개가 전부 산세리프(Space Grotesk + Work Sans)다. 그리고 시안이 촘촘하고
> 읽기 쉬워 보이는 이유의 절반이 폰트였다. 픽셀 폰트는 같은 크기에서 담기는 정보량이 훨씬 적다.

| 쓰는 곳 | 폰트 |
|---|---|
| **화면 전체** | 시스템 산세리프 — **Apple SD Gothic Neo** |
| **앱 이름·로고만** | 갈무리(Galmuri11) — 짧고, 픽셀 정체성이 필요한 유일한 자리 |

갈무리 파일은 그대로 둔다(§8). 로고와, 나중에 픽셀 느낌이 필요한 자리에 쓴다.

### 크기 사다리 (시안 값 그대로)

| 용도 | 크기 / 줄간격 / 굵기 |
|---|---|
큰 제목 (화면 최상단) | **28 / 32 / 700** |
섹션 제목 | **24 / 32 / 600** |
본문 크게 (소개문) | **18 / 28 / 400** |
본문 | **16 / 24 / 400** |
라벨·버튼·칩 | **14 / 20 / 700** |
작은 라벨·배지 | **12 / 16 / 500** |

**위계는 굵기로 만든다.** 700 ↔ 400의 차이가 크기 차이보다 먼저 읽힌다.

⚠️ 시안의 `uppercase`는 쓰지 않는다 — 한글에 대문자가 없다. 대신 **굵기와 자간**으로 라벨을 구분한다.

⚠️ **동적 글자 크기(Dynamic Type)를 지원한다.** 시스템 폰트라 제약 없이 따라간다.

---

## 4. 여백과 격자

**간격은 4의 배수다.**

```
4   8   16   20   32
```

| 용도 | 값 |
|---|---|
아이콘과 라벨 사이 | 8 |
카드 안쪽 여백 | 16 |
카드 사이 | 16 |
섹션 사이 | 32 |
화면 좌우 여백 | 20 |
버튼 높이 | 48 |
탭바 높이 | 80 |

---

## 5. 모양 ★ 2026-08-20 전면 개편

### 핵심 — 테두리는 얇고 그림자는 크다

> **이게 이 디자인의 정체다.** 이전 판은 테두리·그림자를 둘 다 4px로 뒀는데 뭉툭해졌다.
> 시안은 **테두리 2px에 그림자 4~8px**이고, 그 대비가 "종이가 떠 있는" 인상을 만든다.

- **모서리 반경은 0이다.** 예외 없다.
- 테두리와 그림자는 **모두 잉크색**. 그림자는 `blur 0`.

| 대상 | 테두리 | 그림자 |
|---|---|---|
**카드·시트** | 2 | **4 / 4** (대각선) |
**작은 것** — 칩·배지·수집 아이템 | 2 | **2 / 2** |
**강조 상자** — 대화상자·히어로 | 2 | **8 / 8** |
**버튼** | 2 | **0 / 4** ← **아래로만** |
상단바 | 아래 4 | 4 / 4 |
하단 탭바 | 위 4 | 0 / −4 |

**버튼 그림자가 아래로만 향하는 이유.** 카드는 "떠 있는 종이", 버튼은 "눌리는 물건"이다.
누르면 `translateY(+4)`로 내려앉고 그림자가 사라진다. 대각선 그림자면 옆으로 밀리는 느낌이 나서
누르는 동작과 안 맞는다.

### 이중 테두리 — 중요한 상자에만

겉 테두리 **4px**, 안쪽으로 **4px 띄워서 2px** 테두리를 하나 더 그린다.

```
┏━━━━━━━━━━━━━━━━┓   ← 겉 4px
┃ ┌────────────┐ ┃
┃ │  퀘스트 일지  │ ┃   ← 안쪽 2px, 4px 띄움
┃ └────────────┘ ┃
┗━━━━━━━━━━━━━━━━┛
```

화면에서 **가장 중요한 상자 하나**에만 쓴다. 여기저기 쓰면 위계가 사라진다.

### 코너 악센트

큰 상자의 귀퉁이에 **8×8 잉크 사각형**을 박고, 테두리 밖으로 **2px** 빼서 놓는다.

⚠️ **안쪽에 두면 잉크 테두리 위 잉크 점이라 아예 안 보인다** (실제로 겪음).

시안은 대각선 두 곳만 찍은 것과 네 곳 다 찍은 것이 섞여 있다.
**우리는 네 곳으로 통일한다** — 두 곳은 기울어져 보인다.

### 아이콘

⚠️ **SF Symbols·Material Symbols를 쓰지 않는다.** 시안은 Material을 썼지만 도트가 아니다.
필요한 아이콘을 도트로 직접 만든다(`PixelIcon`). 크기 16 또는 24.

필요 목록: 재생 · 멈춤 · 다음 · 사진 · 자물쇠 · 체크 · 위치핀 · 다운로드 · 홈 · 지도 · 사람 ·
뒤로 · 목표(체크박스) · 책 · 헤드폰.

---

## 6. 컴포넌트

### 섹션 헤더 ★ 시안의 가장 특징적인 패턴

```
🎯 임무 목표
─────────────────────    ← 하단 2px 잉크 선, 밑줄까지 8px 여백
```

**아이콘 + 제목(24/600) + 하단 2px 밑줄.** 이 한 세트가 카드마다 반복된다.
시안이 "일지"처럼 보이는 이유가 이 반복이다.

### 카드 (루트·장소)

```
┌──────────────────────────┐  ← 테두리 2, 그림자 4/4, 코너 악센트
│ ┌──────────────────────┐ │
│ │   [실사 사진]          │ │  ← 사진도 테두리 2로 감싼다
│ └──────────────────────┘ │
│ 해안 올레길                │  ← 24 / 600
│ 남부 해안을 따라 걷는 길.    │  ← 16 / 400, 잉크 약함
│                          │
│ 난이도 보통      하루 24km  │  ← 14 / 700
│ ▬▬▬▬▬▬▬▬░░░░░░░░░░       │  ← 막대
│ ▶ 해설 5곳 · 약 4시간      │  ← 14 / 700
│ ┌──────────────────────┐ │
│ │      루트 선택         │ │  ← 버튼, 그림자 아래로만
│ └──────────────────────┘ │
└──────────────────────────┘
```

### 칩 (작은 태그)

`제주도` `하이킹` `2-3시간` 처럼 가로로 나열한다.
**테두리 2 · 그림자 2/2 · 좌우 여백 12 · 상하 6 · 글자 14/700.**

### 등급 배지 — 카드 밖으로 튀어나온 스티커

```
              ┌──────┐
┌─────────────│ 인기 │← 카드 밖 16px, 3도 기울임
│             └──────┘
│  카드 내용
```

**테두리 2 · 그림자 2/2 · 3도 회전.** 회전이 스티커 느낌을 만든다.
카드마다 붙이지 않는다 — 특별한 것 하나에만.

### 막대는 두 종류다

**① 채워지는 막대 — 정도** (난이도, 하루 이동거리, 진행률)

```
┃████████████░░░░░░░░░░░░┃   높이 32
┃      600 / 1000        ┃   ← 막대 안에 텍스트 중앙 배치
```

배경은 **잉크**, 채운 부분은 색. **막대 안에 숫자를 넣는다** (시안 방식).
글자는 채운 색 위·빈 곳 위 모두 읽혀야 하므로 흰색 + 1px 어두운 그림자.

**② 칸으로 나뉜 막대 — 셀 수 있는 것** (이야기 5개 중 3개)

```
■ ■ ■ □ □
```

**⚠️ 섞으면 둘 다 거짓말이 된다.** 셀 수 있는 걸 ①로 그리면 몇 개 남았는지 안 보이고,
정도를 ②로 그리면 없는 눈금을 만들어낸다.

### 대화 상자 (해설 재생 화면)

```
┌────────────────────────────┐  ← 테두리 2, 그림자 8/8, 코너 악센트 4곳
│ ┌────┐ 곱닥이               │  ← 초상 64×64, 테두리 2
│ │초상│ 약 5천년 전, 지하에    │  ← 말하는 속도에 맞춰 찍힌다
│ └────┘ 있던 마그마가_        │
└────────────────────────────┘
   [ 질문하기 ]  [ 다음 ]        ← 버튼 2개
```

**배경은 장소별 픽셀 풍경화** (2026-08-20 결정 — 여기만 예외).
**대본 전문은 시스템 폰트 16/24.** 긴 글이라 읽기 쉬워야 한다.

### 참고사진 마커

실사 사진 위에 도트 마커를 올린다.

- 원: **2px** 테두리, 채우기 없음, 잠김색
- 화살표: 8×8 도트 삼각형
- 라벨: 표면 배경 + 2px 테두리, 12/500

한 사진에 마커는 **3개까지**. 그 이상이면 무엇을 봐야 할지 흐려진다.

---

## 7. 움직임

- 전환 100~150ms, 이징은 단계형(step) 또는 선형
- 흐림·번짐 효과를 쓰지 않는다
- 버튼: 누르면 `translateY(+4)` + 그림자 제거
- 카드: 누르면 `translate(+2,+2)` + 그림자 제거
- 캐릭터는 2~3프레임 반복 (숨 쉬는 정도)
- 화면 전환은 슬라이드보다 **즉시 교체**

⚠️ **"동작 줄이기(Reduce Motion)"** 를 켠 사용자에게는 애니메이션을 끈다.

---

## 8. 필요한 그림 파일

| 무엇 | 사양 | 없으면 |
|---|---|---|
**가이드 캐릭터 (곱닥이)** | 초상 3종(기본·말하는 중·기뻐하는 중). 64×64 기준, 투명 배경 PNG | 대화 상자가 빈다. **대체 불가** |
**앱 아이콘** | 1024×1024 | **앱스토어 제출 불가** |
**해설 배경** | 장소별 픽셀 풍경화. 유료 5곳 × 2장 = 10 | 재생 화면이 빈다 |
**수집물 도트** | 장소당 3~5종 | 수집의 보상감이 사라진다 |
| 장소 썸네일 | **불필요 — KTO `detailImage2` 실사 사진** | |

### 생성 모델을 쓸 때

| 대상 | 방법 |
|---|---|
| 캐릭터·배경 | 생성 모델 ⭕ |
| **"놀멍봅서" 글자** | ❌ → **갈무리 폰트로 직접 조판.** 이미지 모델은 한글을 제대로 쓰지 못한다 |

⚠️ 생성 모델은 "픽셀아트처럼 보이는 고해상도 그림"을 만든다. **후처리 필수:**
① 128×128로 줄인다 → ② 색을 §2 팔레트로 고정한다 → ③ **정수배로** 확대(128×8=1024, 계단식).

### 앱 아이콘 규칙 (놓치면 앱스토어 거절)

- **투명 배경(알파 채널) 금지** — 배경을 색으로 채운다
- **모서리를 미리 둥글게 깎지 않는다** — 애플이 자동으로 깎는다. **정사각으로 제출**
- 도트가 어긋나 보이지 않게 **글자를 중앙에 두고 가장자리 여백을 넉넉히**

---

## 9. 확인이 필요한 것

| 항목 | 왜 |
|---|---|
| 시안 색으로 갈아탈지 | §2 참조. 구조는 색과 독립적이라 언제든 교체 가능 |
| 곱닥이 초상 준비 방법 | 직접/외주/생성. 대화 상자의 임계경로 |
| 실사 사진 고르기 | KTO가 주는 첫 장이 안내판 사진인 경우가 있다(성산 실측). TTS 만든 10곳은 손으로 고르는 게 낫다 |

## 이력

- **2026-08-13:** 최초 작성. 도트 UI 셸 + 실사 사진, 제주 팔레트, 갈무리 폰트, 4px 격자, 반경 0.
- **2026-08-14:** 팔레트 명도 대비 실측(§2) — 다크 모드 강조 배지가 1.26:1이던 결함을 찾아 배지 글자색을 모드 무관 고정 어두운색으로 변경. 갈무리 라이선스 확인(SIL OFL 1.1).
- **2026-08-20 (1):** 테두리·그림자를 4px로 통일하고 코너 악센트를 추가. → 같은 날 재개정으로 대체됨.
- **2026-08-20 (4):** **시안 팔레트 전체를 가져왔다**(§2) — 파랑(`secondary`) 포함, 표면 7단계 · 색 역할 4개 × 컨테이너 · 선 2단계. 라이트는 시안 CSS 그대로, 다크는 시안의 `*-fixed-dim`·`inverse-*`로 파생. **의미 별칭 층**(`accent`·`done`·`locked`)을 둬서 화면 코드가 색 이름을 직접 안 쓰게 했다. 테스트에서 "짝 색은 흰/검정 중 나은 쪽" 검사를 **지웠다** — Material의 `on*`은 일부러 색조를 맞춘 색이라(`onPrimaryContainer`=진한 초록) 그 규칙을 강제하면 시안 색을 바꿔야 한다. 선은 WCAG 1.4.11에 따라 3:1을 적용한다.
- **2026-08-20 (3):** **색까지 시안으로 전면 교체**(§2). 라이트 9색은 시안 CSS 그대로, 다크는 파생 후 실측. 채움색 위 글자를 **짝**(`onPrimary` 등)으로 바꿨다 — 새 팔레트는 주색·잠김의 글자색이 모드마다 반대라 고정색으로는 한쪽이 반드시 깨진다. 08-14의 고정색 해법이 여기서 무효가 됐다.
- **2026-08-20 (2):** **Stitch 시안 4종을 컨셉으로 확정하고 §3·§5·§6을 시안 코드의 실제 CSS 값에 맞춰 다시 썼다.** 바뀐 것: ① 테두리 2px + 그림자 4~8px(둘 다 4px이던 것을 되돌림 — 뭉툭했다) ② 버튼 그림자는 아래로만 ③ 이중 테두리 상자 ④ 섹션 헤더 패턴(아이콘+제목+밑줄) ⑤ 칩·등급 배지(3도 회전) ⑥ 막대 안에 텍스트 ⑦ **UI 본문 폰트를 픽셀 → 시스템 산세리프**로. 갈무리는 로고에만 남긴다. 색은 시안(Material 자동생성) 대신 **제주 팔레트를 유지** — 실사 썸네일과 채도를 맞추려 고른 색이고 대비를 실측해 뒀다. 카드 썸네일은 **실사 사진(KTO `detailImage2`)** 으로 확정.
